# AUTHORITATIVE causal rebuild — session 02

New features **and** new pregame cohort. This is the run
`reports/POST_LEAK_FIX_COMPARISON.md` scores, and the one session 03A should pin
into `releases/current_baseline.json`.

Not yet served: `releases/current_baseline.json` still pins `_20260809`. Nothing
reads these files until 03A re-freezes. Paths and SHA-256 digests are listed in
the comparison report.

See `../rebuild_20260809T220500/README.md` — that run is the ablation control and
must not be deleted.
