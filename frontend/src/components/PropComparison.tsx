/**
 * PropComparison — Phase 4 placeholder.
 *
 * Prop line comparison requires a live market odds feed (The Odds API key).
 * This component renders nothing until Phase 4 wires the /odds endpoint.
 * The PropComparison dataclass is retained in services/projection.py for
 * future wiring.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-function
export function PropComparison(): null {
  return null
}
